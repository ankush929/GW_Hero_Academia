/// @description Updates

depth = -1*(y + sprite_height - 1);
image_index = resistance;

if destroy and instance_exists(collisionBox)
{
	with(collisionBox)
		instance_destroy();		// We destroy de collision box...
	instance_destroy();
}
